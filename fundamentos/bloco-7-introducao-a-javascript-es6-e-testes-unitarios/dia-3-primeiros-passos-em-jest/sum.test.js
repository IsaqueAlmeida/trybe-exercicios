/* const sum = require('./sum');

describe('Requisito 1.1', () => {
  test('Analisando para ver se a soma de 4 + 5 = 9', () => {
    expect(sum(4, 5)).toBe(9);
  });
});

describe('Requisito 1.2', () => {
  test('Verificando de 0 + 0 = 0', () => {
    expect(sum(0, 0)).toBe(0);
  })
});

describe('Requisito 1.3', () => {
  test('Verificando se é um número', () => {
    expect(() => { sum(4, '5') }).toThrow();
  });
});

describe('Requisito 1.4', () => {
  test('Verificando a mensagem de erro', () => {
    expect(() => { sum(4, '5') }).toThrowError(new Error('parameters must be numbers'));
  });
});

 */